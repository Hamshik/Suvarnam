// src/frontend/parser/parser_includes.h
#ifndef PARSER_INCLUDES_H
#define PARSER_INCLUDES_H

#include "parser/parser_helpers.h"
#include "shared/structs.h"
#include "ast/ast.h"

#define NODE(x) (x)
#define TYPE(x) ((x))
#define PARAMS(x) ((x))
#define IDX(x) ((x))
#define SIZE(x) ((x))
#define DATA(x) ((x))

#endif